

When generating the [hook-name].doc.ts
- keep api description short
  
When generating the [hook-name].example.ts
- Don't do "real life exemple" instead keep as demo that demo the feature
- Simple and compact design